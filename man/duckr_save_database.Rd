% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{duckr_save_database}
\alias{duckr_save_database}
\title{Save an in-memory database to a file}
\usage{
duckr_save_database(file, dir = ".", con = duckr_con(), overwrite = FALSE)
}
\arguments{
\item{file}{Output \code{.duckdb} file name.}

\item{dir}{Directory to write \code{file} into. Defaults to \code{"."}.}

\item{con}{A DBI connection. Defaults to the current connection.}

\item{overwrite}{If \code{FALSE} (default) error when the output file exists; if
\code{TRUE} overwrite it.}
}
\value{
The connection \code{con}, invisibly.
}
\description{
Persists the whole current in-memory DuckDB database to a \code{.duckdb} file by
attaching the target file and copying every catalog object (tables and
views) into it. The live connection is left untouched and still in memory.
}
\details{
Only useful for an in-memory database: when the connection is already backed
by a file, nothing is copied and a warning reports where the database
already lives.
}
\examples{
con <- duckr_connect()
duckr_add_df(mtcars, name = "cars")
duckr_save_database("backup.duckdb", dir = tempdir())
duckr_close()
}
\seealso{
Other data exporters:
\code{\link[=duckr_to_csv]{duckr_to_csv()}},
\code{\link[=duckr_to_parquet]{duckr_to_parquet()}}
}
\concept{data exporters}
