% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export.R
\name{duckr_to_csv}
\alias{duckr_to_csv}
\title{Export a table or view to a CSV file}
\usage{
duckr_to_csv(
  name,
  file,
  dir = ".",
  con = duckr_con(),
  delim = NULL,
  header = TRUE,
  overwrite = FALSE
)
}
\arguments{
\item{name}{Name of the table or view to export.}

\item{file}{Output CSV file name.}

\item{dir}{Directory to write \code{file} into. Defaults to \code{"."}.}

\item{con}{A DBI connection. Defaults to the current connection.}

\item{delim}{Field delimiter. \code{NULL} (default) uses DuckDB's default (\verb{,}); a
string forces the delimiter.}

\item{header}{Whether to write a header row. Defaults to \code{TRUE}.}

\item{overwrite}{If \code{FALSE} (default) error when the output file exists; if
\code{TRUE} overwrite it.}
}
\value{
The connection \code{con}, invisibly.
}
\description{
Copies the content of a table or view of the connection to a CSV file.
}
\examples{
con <- duckr_connect()
duckr_add_df(mtcars, name = "cars")
duckr_to_csv("cars", "cars.csv", dir = tempdir())
duckr_close()
}
\seealso{
Other data exporters:
\code{\link[=duckr_save_database]{duckr_save_database()}},
\code{\link[=duckr_to_parquet]{duckr_to_parquet()}}
}
\concept{data exporters}
