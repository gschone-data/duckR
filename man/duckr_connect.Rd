% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/connection.R
\name{duckr_connect}
\alias{duckr_connect}
\title{Open a DuckDB connection}
\usage{
duckr_connect(dbdir = ":memory:", mem_fraction = 0.75, threads = NULL)
}
\arguments{
\item{dbdir}{Path to a \code{.duckdb} file for a persistent database, or
\code{":memory:"} (the default) for an in-memory database.}

\item{mem_fraction}{Fraction of total RAM (0-1) allocated to DuckDB's
\code{memory_limit}. Converted to an absolute value because DuckDB does not
accept percentages. If RAM cannot be detected, DuckDB's default limit is
kept and a warning is emitted.}

\item{threads}{Number of threads. \code{NULL} (the default) keeps DuckDB's native
behaviour (all cores); an integer caps the thread count.}
}
\value{
The DBI connection object, invisibly. It is also stored as the
current connection (see \code{\link[=duckr_con]{duckr_con()}}).
}
\description{
Opens a DuckDB connection, applies a memory limit derived from the machine's
RAM, optionally caps the number of threads, and registers the connection as
the current one used by the other \verb{duckr_*} functions.
}
\examples{
con <- duckr_connect()
duckr_status()
duckr_close()
}
\seealso{
Other connection functions:
\code{\link[=duckr_close]{duckr_close()}},
\code{\link[=duckr_close_all]{duckr_close_all()}},
\code{\link[=duckr_con]{duckr_con()}}
}
\concept{connection functions}
