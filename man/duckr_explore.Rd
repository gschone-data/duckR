% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore.R
\name{duckr_explore}
\alias{duckr_explore}
\title{List tables and views}
\usage{
duckr_explore(con = duckr_con(), row_count = TRUE)
}
\arguments{
\item{con}{A DBI connection. Defaults to the current connection.}

\item{row_count}{If \code{TRUE} (default) run \verb{SELECT count(*)} per object; if
\code{FALSE}, \code{n_rows} is \code{NA}. Counting can be expensive on attached
PostgreSQL databases or views over large files.}
}
\value{
A data frame with columns \code{catalog}, \code{schema}, \code{name}, \code{type}
(\code{"table"} or \code{"view"}) and \code{n_rows}.
}
\description{
Lists tables and views across all catalogs (the in-memory/file database plus
any attached database, including PostgreSQL), with an optional row count.
}
\examples{
con <- duckr_connect()
duckr_add_df(mtcars, name = "cars")
duckr_explore()
duckr_close()
}
\seealso{
Other exploration functions:
\code{\link[=duckr_status]{duckr_status()}}
}
\concept{exploration functions}
